# ------------------------------------------------------------------
# ALL IN ONE VASTGOEDONDERHOUD - environment variables
# Kopieer naar .env.local (lokaal) en zet dezelfde keys in Vercel.
# NOOIT committen: .env.local staat in .gitignore.
# ------------------------------------------------------------------

# Publieke site-URL (zonder trailing slash) - voor canonical, OG, sitemap
NEXT_PUBLIC_SITE_URL=https://www.uw-domein.nl

# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# Alleen server-side! Wordt gebruikt in route handlers voor inserts en uploads.
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Resend (e-mail) - https://resend.com
RESEND_API_KEY=re_...
# Afzender. Domein moet geverifieerd zijn in Resend. Tijdens testen: onboarding@resend.dev
EMAIL_FROM="All in One Vastgoedonderhoud <offerte@uw-domein.nl>"
# Waar nieuwe aanvragen en contactberichten naartoe gaan
QUOTE_NOTIFICATION_EMAIL=info@example.nl
# Bevestigingsmail naar de klant (true/false, standaard true)
SEND_CUSTOMER_CONFIRMATION=true

# Dashboard (/admin). Alleen deze e-mailadressen (Supabase Auth-gebruikers) mogen inloggen.
# Meerdere adressen scheiden met een komma. Leeg = niemand kan inloggen.
ADMIN_EMAILS=info@example.nl

# Optioneel: basis-URL van het dashboard voor de "Bekijk aanvraag"-link in de notificatiemail.
# Standaard: NEXT_PUBLIC_SITE_URL + /admin
# ADMIN_URL=https://www.uw-domein.nl/admin

# Optioneel: Google-reviews automatisch ophalen (Places API (New), zie docs/DEPLOYMENT.md 4)
# GOOGLE_PLACES_API_KEY=AIza...
# GOOGLE_PLACE_ID=ChIJ...
# Optioneel: geheim voor de dagelijkse verversing via Vercel Cron (willekeurige lange string).
# Vercel stuurt dit automatisch mee als Authorization-header naar /api/cron/google-reviews.
# CRON_SECRET=verander-dit-in-een-lange-willekeurige-string

# Optioneel: salt voor het hashen van IP-adressen (AVG). Willekeurige lange string.
IP_HASH_SALT=verander-dit-in-een-lange-willekeurige-string

# Optioneel: hero-video (Higgsfield). Pad in /public of volledige URL. Leeg = statische poster.
# NEXT_PUBLIC_HERO_VIDEO_SRC=/videos/hero.mp4
